from sklearn.datasets import load_iris
from sklearn.tree import DecisionTreeClassifier, export_text

data = load_iris()
X = data.data
y = data.target

model = DecisionTreeClassifier()
model.fit(X, y)

tree_rules = export_text(model, feature_names=data.feature_names)
print(tree_rules)

sample = [[5.1, 3.5, 1.4, 0.2]]
pred = model.predict(sample)
print("Prediction for", sample, ":", data.target_names[pred][0])


OUTPUT
|--- petal width (cm) <= 0.80
|   |--- class: 0
|--- petal width (cm) >  0.80
|   |--- petal width (cm) <= 1.75
|   |   |--- petal length (cm) <= 4.95
|   |   |   |--- class: 1
|   |   |--- petal length (cm) >  4.95
|   |   |   |--- class: 2
|   |--- petal width (cm) >  1.75
|   |   |--- class: 2

Prediction for [[5.1, 3.5, 1.4, 0.2]] : setosa
